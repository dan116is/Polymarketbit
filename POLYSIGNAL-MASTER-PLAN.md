# POLYSIGNAL — MASTER PLAN
## אפליקציית איתותים לשוקי ה-5 דקות של Polymarket (BTC Up/Down)

**איך משתמשים בקובץ הזה:** מדביקים אותו ב-Claude Code יחד עם `MASTER-PROMPT-AUTONOMOUS.md`. הקובץ הזה הוא ה-INIT המלא — GOAL, DONE, BOUNDARIES, BUDGET, ארכיטקטורה ואבני דרך. הסוכן רץ לפי הפרוטוקול; הקובץ הזה מגדיר את הפרויקט.

---

# 0. עובדות קשות — קודקוד התוכנית, לא הערת שוליים

1. **אין מודל AI שמנבא כיוון BTC ב-5 דקות.** תנועה כזו היא רעש. כל "סוכן חכם" שמצביע Up/Down מתוך "הבנה" הוא מטבע עם UI. לכן הליבה כאן היא **מנוע fair-value דטרמיניסטי**, לא LLM.
2. **מקור האדג' היחיד:** פער בין ההסתברות המתמטית של Up (נגזרת ממחיר פתיחת החלון, הספוט החי, הזמן שנותר והתנודתיות) לבין המחיר המצוטט ב-order book. אין פער מעל סף → **PASS**. צפי ריאלי: PASS ברוב מוחלט של החלונות.
3. **לחיצה ידנית = 2–5 שניות latency.** האפליקציה מציגה רק איתותים שמחזיקים מעמד בסקאלת שניות (בעיקר בשלהי החלון). הבזקי latency שייכים לבוט (M6), לא ליד.
4. **בהימורי $1/$2/$5 זה פרויקט R&D, לא מנוע רווח.** הערך: תשתית, דאטה מלוגג, ומסלול השלמה לבוט אוטומטי. כל טענה אחרת = שיווק ריק.
5. **שום מצב LIVE לא נפתח בלי שני שערים כמותיים ירוקים** (Backtest → Paper). מספרים בסעיף 6. אין עקיפה.

---

# 1. INIT

## 1.1 GOAL
לבנות מערכת איתותים אישית — שרת + אפליקציית PWA בטלפון + התראות Telegram — שמנטרת בזמן אמת את שוקי ה-BTC Up/Down של 5 דקות ב-Polymarket, מחשבת fair value מול המחיר המצוטט, ומציגה **UP / DOWN / PASS + גודל הימור מומלץ ($1/$2/$5)**, כשהיא נפתחת למצב LIVE רק אחרי הוכחת EV חיובי בבדיקה היסטורית ובריצת paper.

## 1.2 DONE — קריטריונים בינאריים (המערכת כולה)
- [ ] זיהוי דטרמיניסטי של השוק הפעיל עובד על ≥20 חלונות רצופים, 0 החמצות (ראיה: לוג).
- [ ] המנוע רץ 24 שעות רצוף, uptime ≥ 99%, התאוששות אוטומטית מניתוק WebSocket (ראיה: לוג uptime).
- [ ] כל חלון נרשם ב-SQLite עם כל השדות שבסעיף 4.4 (ראיה: שאילתת ספירה מול מספר החלונות בפועל).
- [ ] דוח Backtest מלא על הדאטהסט ההיסטורי הופק, וספי האיתות נבחרו מהדאטה — לא מנוחשים (ראיה: `reports/backtest.md`).
- [ ] **GATE 1 ירוק** (סעיף 6) או ריצה נעצרה עם דוח NO-GO.
- [ ] התראת Telegram מגיעה לטלפון ≤ 2 שניות p95 מרגע האיתות (ראיה: מדידת round-trip בלוג).
- [ ] ה-PWA נטען בטלפון, מציג מצב חי (חלון, ספירה לאחור, P_fair, מחיר שוק, EV, המלצה), 0 שגיאות console (ראיה: screenshot מהטלפון).
- [ ] **GATE 2 ירוק** (סעיף 6) לפני שהמערכת מציגה המלצת LIVE כלשהי.
- [ ] מעקות הסיכון מוכחים: הפרה מדומה של תקרת הפסד יומית נועלת את המערכת ל-24 שעות (ראיה: טסט).
- [ ] אפס נתונים מקובעים בקוד שצריכים להיקרא מה-API (עמלה, tick size, tokenIds) — הכול נשלף בזמן ריצה (ראיה: grep + קוד).
- [ ] אין MOCK בתוצר הסופי.

## 1.3 BOUNDARIES — אל-געת
- **הקוד לא מבצע שום פעולת מסחר בכסף אמיתי.** שליחת הוראות (M6) — רק באישור מפורש נפרד, לעולם לא כברירת מחדל.
- לא נוגעים בשום מערכת קיימת אחרת של דניאל (ERP, Shopips, Bnext, בוטים קיימים). פרויקט מבודד בתיקייה משלו.
- מפתחות וטוקנים רק ב-`.env`, לעולם לא בקוד ולא ב-git.
- גדלי הימור מותרים בהמלצות: $1 / $2 / $5 בלבד. אין המלצה מעל $5 בשום מצב.
- תקרת הפסד יומית קשיחה במעקב: ‎-$10 → נעילת איתותי LIVE ל-24 שעות. הערך ניתן לשינוי רק ע"י דניאל ידנית ב-config.
- אסור להציג המלצת LIVE לפני ששני ה-GATES ירוקים. אין override בקוד.
- אין martingale, אין הכפלות אחרי הפסד, אין יותר מפוזיציה מומלצת אחת לחלון.

## 1.4 BUDGET
- מקסימום איטרציות: 25
- checkpoint: כל 5 איטרציות
- ערוץ הסלמה: Telegram (הערוץ הקיים של דניאל)
- בעיה בודדת: מקסימום 20% מהתקציב לפני הסלמה
- איטרציות מודל ב-GATE 1 (סעיף 6): מקסימום 3 לפני עצירה ודוח NO-GO

## 1.5 יכולת אימות — קיימת מקצה לקצה
- **דאטה היסטורי אמיתי לבדיקה:** דאטהסט ציבורי (CC0) של ~89,000 שווקי 5-דקות שנפתרו, tick שניתי של top-of-book, BTC ממרץ–מאי 2026: `huggingface.co/datasets/kachoio/polymarket-5-minute-crypto-up-down-markets` (קבצי `btc_markets.parquet`, `btc_ticks.parquet`). דאטהסט משלים עם עסקאות + תוצאות + נרות Binance: `huggingface.co/datasets/BrockMisner/polymarket-crypto-5m-15m`.
- **אימות חי:** ה-APIs ציבוריים לקריאה — Gamma (מטא-דאטה), CLOB REST + WebSocket (ספרים), Binance WS (ספוט). curl + לוגים + screenshots.
- **אימות מובייל:** הטלפון של דניאל — screenshot של ה-PWA + מדידת זמן הגעת התראה.

---

# 2. ארכיטקטורה

```
┌──────────────────────────── שרת (מחשב Windows קבוע של דניאל) ────────────────────────────┐
│                                                                                          │
│  WATCHER            QUANT                    RISK                DELIVERY                │
│  ┌───────────┐      ┌──────────────────┐     ┌─────────────┐     ┌────────────────────┐  │
│  │ Binance WS │──┐   │ P_up = Φ(ln(S/S₀)│     │ סף EV        │     │ Telegram Bot API   │  │
│  │ btcusdt    │  ├──▶│      /(σ·√τ))    │────▶│ סולם $1/2/5  │────▶│ (התראה + צליל)     │  │
│  │ CLOB WS    │──┘   │ σ = EWMA שניתי   │     │ stop-loss    │     │ PWA (WebSocket)    │  │
│  │ order book │      │ EV מול bid/ask   │     │ cooldown     │     │ כרטיס UP/DOWN/PASS │  │
│  │ Gamma/slug │      │ + עמלה מה-API    │     │ נעילת LIVE   │     └────────────────────┘  │
│  └───────────┘      └──────────────────┘     └─────────────┘                             │
│         │                                                                                │
│         ▼                                                                                │
│  SQLite: כל tick מסונן, כל איתות, כל תוצאה  ──▶  ANALYST (LLM, לא בזמן אמת):             │
│                                                  דוח כיול יומי + הצעות סף — לאישור דניאל │
└──────────────────────────────────────────────────────────────────────────────────────────┘
```

## 2.1 חלוקת ה"סוכנים" — איפה AI כן ואיפה לא
| סוכן | מהות | AI? | למה |
|---|---|---|---|
| WATCHER | פידים, זיהוי שוק, state per window | לא — דטרמיניסטי | פיד זה צנרת, לא שיפוט |
| QUANT | fair value + EV | לא — נוסחה סגורה | LLM בנתיב ה-tick = איטי, יקר, ולא מוסיף דיוק. מתמטיקה מנצחת כאן תמיד |
| RISK | סולם הימורים, נעילות | לא — חוקים קשיחים | חוקי כסף לא נתונים לשיקול דעת של מודל |
| ANALYST | דוח כיול יומי: Brier, EV לפי סף/זמן-בחלון, אנומליות, הצעות שיפור | **כן — Claude, offline** | כאן LLM שווה משהו: קריאת לוגים, ניסוח מסקנות, הצעת ניסויים. כל הצעה → אישור ידני |
| BUILDER | הלולאה שבונה את המערכת | **כן — Claude Code** | זה ה-master prompt עצמו |

זה מה ש"סוכנים חכמים" אומר כאן בפועל. סוכן שמנבא כיוון — לא קיים בתוכנית, בכוונה.

## 2.2 סטאק
- **Python 3.11+** — האקוסיסטם כאן פייתוני: `py-clob-client` (רשמי, יידרש רק ב-M6), `websockets`, `pandas`/`pyarrow` ל-backtest על parquet, `python-telegram-bot`.
- **SQLite** — קובץ אחד, אפס תחזוקה. סטנדרט קיים אצל דניאל.
- **PWA** — קובץ HTML בודד (vanilla JS + WebSocket לשרת), מוגש מאותו פרוצס (aiohttp/FastAPI). מסך אחד: כרטיס ענק UP/DOWN/PASS, ספירה לאחור, P_fair מול מחיר, EV בסנטים, המלצת סכום. Add-to-Home-Screen בטלפון = "האפליקציה הפרטית".
- **הרצה קבועה:** Task Scheduler + `.bat` (הדפוס הקיים של דניאל) או NSSM כ-service. auto-restart בקריסה.

## 2.3 עובדות API מאומתות (נכון ליולי 2026) — לאימות מחדש ב-DISCOVER
- השווקים הושקו ב-13.2.2026; רצים על חלונות קבועים של 300 שניות.
- **זיהוי שוק דטרמיניסטי, בלי לחפש:** `window_ts = now - (now % 300)` → slug בפורמט `btc-updown-5m-{window_ts}`. Gamma API על ה-slug מחזיר את השוק ואת `clobTokenIds` של Up/Down. אפס תלות בעיכובי אינדוקס.
- מחירים חיים: CLOB REST (`/book`) + CLOB WebSocket (ערוץ order book ציבורי).
- **מקור ה-resolution: Chainlink BTC/USD Data Stream** — לא Binance. מחיר הפתיחה של החלון הוא מחיר האורקל, וגישה ישירה ל-Data Streams דורשת אימות. ⚠ משימת DISCOVER קריטית: לאתר את מקור מחיר הפתיחה/האורקל החי שפוליmarket חושפת (RTDS WebSocket / שדה בשוק עצמו), ולמדוד את הבסיס Binance↔אורקל. עד אז — Binance כ-proxy עם באפר בסף.
- עמלות מסחר קיימות בשווקים האלה. **אסור לקבע מספר** — לקרוא מה-API/מסמכים בזמן ריצה ולכלול ב-EV.
- שוברי שוויון: מחיר סגירה ≥ פתיחה → Up (הטיה מיקרוסקופית ל-Up, מובאת בחשבון, לא נסחרת לבדה).

---

# 3. המודל — QUANT

## 3.1 fair value
תחת הילוך מקרי ללא drift (ההנחה הנכונה ל-5 דקות):

```
P_up = Φ( ln(S_t / S_open) / (σ · √τ) )
```
- `S_t` — ספוט נוכחי (Binance, עם באפר בסיס מול האורקל)
- `S_open` — מחיר פתיחת החלון לפי מקור ה-resolution
- `τ` — זמן שנותר בחלון (שניות, מנורמל)
- `σ` — תנודתיות מ-EWMA של תשואות לוג שניתיות (חלון גלגול ~10 דק', half-life נקבע ב-backtest)

## 3.2 EV ואיתות
```
edge_up   = P_up   − ask_up   − fee − buffer
edge_down = (1−P_up) − ask_down − fee − buffer
```
- איתות רק אם `edge > θ`. ערך פתיחה: **θ = 6¢**, `buffer` = בסיס אורקל + מרווח ביצוע-יד. **שני הערכים נקבעים סופית ב-backtest, לא כאן.**
- מסנן זמן: איתותים ליד רק כשנותרו **15–120 שניות** בחלון (מוקדם מדי = הפער נסגר לפני הלחיצה; מאוחר מדי = אין זמן ללחוץ). הטווח המדויק — מה-backtest.
- אין עומק מספיק בספר למילוי $5 במחיר המצוטט → האיתות יורד דרגה או הופך PASS.

## 3.3 סולם ההימור (ממופה לכפתורים הקיימים)
| edge נטו | המלצה |
|---|---|
| θ עד θ+4¢ | $1 |
| θ+4¢ עד θ+8¢ | $2 |
| מעל θ+8¢ | $5 — **רק אחרי GATE 2** |

---

# 4. אבני דרך — פרוסות אנכיות (PLAN.md נגזר מכאן)

## M0 — Discovery Spike (הכי מסוכן רץ ראשון)
סקריפט בודד שמדפיס בזמן אמת: השוק הפעיל (slug דטרמיניסטי) → tokenIds → book חי → **מחיר פתיחת החלון ממקור ה-resolution**.
**DONE:** רץ 30 דקות, מזהה כל חלון חדש בתוך ≤2 שניות מפתיחתו, מדפיס open+book לכל חלון; שאלת מקור-הפתיחה סגורה או מסומנת ⚠ עם ממצאים.
*אם אין דרך אמינה למחיר הפתיחה — הסלמה מיידית. זה ה-blocker היחיד שיכול להרוג את הפרויקט, ולכן הוא ראשון.*

## M1 — מנוע איתותים ל-console
Binance WS + CLOB WS + המודל מסעיף 3 → שורת מצב חיה + רישום SQLite.
**DONE:** שעה רצופה, כל חלון נרשם, ריצה כפולה לא מכפילה רשומות, ניתוק WS מתאושש לבד.

## 4.4 סכמת לוג (חובה מ-M1)
`window_ts, s_open, source_open, s_close, outcome, p_fair@signal, ask_up, ask_down, bid_up, bid_down, fee, edge, signal(UP/DOWN/PASS), stake_reco, t_remaining@signal, latency_ms, mode(BACKTEST/PAPER/LIVE), acted(bool, ידני), pnl`

## M2 — Backtest Harness → **GATE 1**
מנוע replay שמריץ את המודל על `btc_ticks.parquet` (89K חלונות), סורק רשת ספים (θ, טווח זמן, half-life של σ, buffer) ומפיק `reports/backtest.md`: EV/איתות, hit-rate מול מחיר, Brier, פילוח לפי זמן-בחלון, עקומת רגישות ל-latency ביצוע (0/2/5 שניות).
**DONE:** הדוח קיים; הספים נבחרו מהדאטה; סימולציית latency של 5 שניות כלולה (כי היד איטית).

## M3 — Delivery: Telegram + PWA
**DONE:** התראה בטלפון ≤2s p95; PWA על מסך הבית; screenshot; 0 שגיאות console; מצב שרת-נפל מוצג ב-PWA (לא מסך קפוא שנראה חי).

## M4 — Paper Loop → **GATE 2**
המערכת רצה 5–7 ימים במצב PAPER: מאותתת, לא ממליצה בכסף, רושמת מה היה קורה כולל latency יד מדומה (5s).
**DONE:** ≥200 איתותים או 7 ימים (המוקדם); דוח כיול; GATE 2 מחושב.

## M5 — LIVE (איתותים בלבד, יד של דניאל לוחצת)
נפתח אוטומטית רק אם GATE 1+2 ירוקים. RISK אוכף: סולם סכומים, stop-loss יומי ‎-$10 → נעילה 24h, cooldown של 2 חלונות אחרי 2 הפסדים רצופים, kill-switch בפקודת Telegram.
**DONE:** טסט נעילה עובר; שבוע LIVE ראשון עם דוח יומי של ANALYST.

## M6 — (אופציונלי, החלטה נפרדת) בוט ביצוע אוטומטי
`py-clob-client`, אותם GATES, סכומים זהים. זה המוצר עם הסיכוי האמיתי היחיד לאדג' — כי הוא מוחק את ה-5 שניות של היד. **לא מתחיל בלי אישור מפורש — נמצא מעבר ל-BOUNDARY של כסף.**

---

# 5. לולאת-העל — מיפוי לפרימיטיבים של Claude Code

| שלב | פרימיטיב | תנאי עצירה |
|---|---|---|
| M0–M3 (בנייה) | `/goal` נפרד לכל אבן דרך, עם ה-DONE הבינארי שלה | "stop after 5 tries" לכל goal |
| M2 סריקת ספים | dynamic workflow: worker לכל אזור פרמטרים, במקביל, על פרוסת פיילוט קודם | כל worker עם goal משלו |
| M4 Paper | המערכת עצמה היא הלולאה (process קבוע); `/schedule` יומי מריץ את ANALYST על הלוג | 7 ימים או 200 איתותים |
| M5 LIVE | process קבוע + ANALYST יומי מתוזמן | stop-loss / kill-switch / דניאל |
| כשל חוזר | פרוטוקול תקיעה של ה-MASTER-PROMPT: 3 כשלונות → אבחון שורש, לא ניסיון רביעי | הסלמה ל-Telegram |

**כלל הזהב של הלולאה:** GATE אדום זו לא תקלה לתקן בכוח — זו תוצאה. אם אחרי 3 איטרציות מודל GATE 1 עדיין אדום, הלולאה עוצרת ומוסרת דוח NO-GO עם הראיות. עצירה נקייה עם דוח = הצלחה של הפרוטוקול.

---

# 6. שערי GO/NO-GO — המספרים

## GATE 1 — Backtest (סוף M2)
על הקונפיגורציה הנבחרת, **כולל סימולציית latency של 5 שניות ועמלה בפועל**:
- EV ממוצע לאיתות ≥ **+2¢ לשער**
- מספר איתותים בתקופת הבדיקה ≥ **500** (פחות מזה = אין מובהקות, לא משנה כמה ה-EV יפה)
- Brier של P_fair בכל רגעי האיתות ≤ **0.20**
- ה-EV נשאר חיובי גם כשה-latency מוכפל ל-10 שניות (מבחן שבריריות)

**אדום → עד 3 איטרציות מודל → עדיין אדום → NO-GO.** משמעות NO-GO: היד האנושית איטית מדי לאדג' הקיים; הנתיב היחיד קדימה הוא M6 (בוט), בהחלטה נפרדת. זו תוצאה לגיטימית וסבירה — להגיד אותה בקול, לא לרכך.

## GATE 2 — Paper (סוף M4)
- ≥ **200** איתותים מלוגגים
- EV ממומש ממוצע ≥ **+1.5¢ לשער** אחרי עמלות
- סטיית הכיול: |ממוצע P_fair − שיעור פגיעה בפועל| ≤ **7 נק' אחוז**
- אפס הפרות של חוקי RISK בלוג

**אדום → LIVE לא נפתח. נקודה.**

---

# 7. חוקי דאטה (ירושה מה-MASTER-PROMPT + ספציפי)
- אין המצאת מחירים, עמלות, עומק ספר או תוצאות. הכול מה-API או מהדאטהסט, עם מקור בלוג.
- Binance הוא proxy, לא מקור אמת. מקור האמת ל-resolution הוא אורקל Chainlink — ההפרש נמדד, לא מונח.
- כל מספר בדוחות ANALYST ניתן למעקב לשאילתת SQLite שמופיעה בדוח.
- ספק אם נתון אמיתי או הנחה → הנחה, מסומן.

---

# 8. תבנית הפעלה — להדביק ב-Claude Code

```
טען את MASTER-PROMPT-AUTONOMOUS.md ואת POLYSIGNAL-MASTER-PLAN.md ופעל לפיהם.

GOAL: כמוגדר בסעיף 1.1 של POLYSIGNAL-MASTER-PLAN.md.
DONE: הרשימה בסעיף 1.2, בתוספת ה-DONE הפרטני של כל אבן דרך בסעיף 4.
BOUNDARIES: סעיף 1.3. שים לב במיוחד: אפס מסחר בכסף אמיתי ע"י קוד; M6 מחוץ לסקופ עד אישור נפרד.
BUDGET: סעיף 1.4.

סדר ריצה: M0 → M1 → M2 → GATE 1 → M3 → M4 → GATE 2 → M5.
GATE אדום = עצירה + דוח, לא עקיפה.

צא לדרך.
```

---

# 9. מה נשאר פתוח בכוונה (משימות DISCOVER של הסוכן)
1. מקור זמן-אמת למחיר פתיחת חלון לפי האורקל (RTDS / שדה שוק / Data Streams) — ה-blocker הפוטנציאלי היחיד, רץ ראשון ב-M0.
2. שיעור העמלה בפועל בשווקים האלה — נקרא מה-API, נכנס ל-EV.
3. tick size ועומק טיפוסי בספר — קובע אם $5 בכלל מתמלא במחיר המצוטט.
4. חוקיות/מיסוי שימוש בפלטפורמה מהמיקום שלך — באחריות דניאל, לא בסקופ הסוכן. שורה אחת, פעם אחת.
